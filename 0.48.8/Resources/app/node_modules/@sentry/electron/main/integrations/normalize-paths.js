const core = require('@sentry/core');
const electron = require('electron');
const normalize = require('../normalize.js');

const normalizePathsIntegration = core.defineIntegration(() => {
    return {
        name: 'NormalizePaths',
        processEvent(event) {
            return normalize.normalizePaths(event, electron.app.getAppPath());
        },
    };
});

exports.normalizePathsIntegration = normalizePathsIntegration;//# sourceMappingURL=https://cursor-sourcemaps.s3.amazonaws.com/sourcemaps/7801a556824585b7f2721900066bc87c4a09b740/node_modules/@sentry/electron/main/integrations/normalize-paths.js.map
